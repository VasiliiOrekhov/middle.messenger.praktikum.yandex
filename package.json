{
  "name": "repo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "open": "start http://localhost:3000",
    "server": "node server.js",
    "dev": "vite --port 3000",
    "build": "vite build",
    "start": "npm run build  && npm run open && npm run server",
    "preview": "vite preview",
    "test": "npm run build  && npm run server",
    "test2": "npm run build  && npm run server && npm run open"
  },
  "engines": {
    "node": "18.13.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.18",
    "sass": "^1.68.0",
    "vite": "^4.4.5"
  },
  "dependencies": {
    "express": "^4.18.2",
    "handlebars": "^4.7.8",
    "vite-plugin-handlebars": "^1.6.0"
  }
}
